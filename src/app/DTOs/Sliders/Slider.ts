export class Slider {
    constructor(
        id: number,
        imageName: string,
        title: string,
        description: string,
        link: string,
        isDelete: boolean,
        createDate: Date,
        lastUpdateDate: Date
    ) {

    }
}